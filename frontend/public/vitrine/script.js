const header=document.querySelector('.header');const nav=document.querySelector('.nav');const menu=document.querySelector('.menu');const progress=document.querySelector('.progress');const topBtn=document.querySelector('.top');const links=[...document.querySelectorAll('.nav a')];const sections=[...document.querySelectorAll('section[id]')];function onScroll(){const y=scrollY;header.classList.toggle('scrolled',y>20);topBtn.classList.toggle('show',y>520);const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=h?Math.min(y/h*100,100)+'%':'0%';let current=sections[0]?.id;sections.forEach(s=>{if(s.offsetTop<=y+150)current=s.id});links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+current||a.getAttribute('href')==='#solutions'&&['services','securite'].includes(current)))}addEventListener('scroll',onScroll,{passive:true});onScroll();menu.addEventListener('click',()=>nav.classList.toggle('open'));links.forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelector('.top').addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));const obs=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');obs.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));document.querySelector('form')?.addEventListener('submit',e=>{e.preventDefault();document.querySelector('.msg').textContent='Votre demande est prête. Le formulaire peut ensuite être connecté au backend.';e.target.reset()});