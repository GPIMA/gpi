[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "php artisan config:clear && php artisan migrate --force && (php artisan db:seed --force || true) && php artisan serve --host 0.0.0.0 --port ${PORT:-8080}"
restartPolicyType = "ON_FAILURE"
